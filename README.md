# Job Application Work Horror

DirectX 11 / C++ で制作した、一人称視点の短編心理ホラーゲームです。  
暗い施設を探索し、懐中電灯の電池を管理しながら異常と手掛かりを見つけ、2つのステージから脱出します。

## ゲームの流れ

1. Stage 1で3本のヒューズを探し、電力を復旧する
2. 復旧した照明と変化した環境を手掛かりに出口へ向かう
3. Stage 2のループする廊下で異常を観察し、正しい順番で行動する
4. 最終扉を開けてResultへ到達する

目的を見失った場合は、キーボードの`H`またはXboxコントローラーの`LB`でヒントを確認できます。

## 操作方法

| 操作 | キーボード・マウス | Xboxコントローラー |
| --- | --- | --- |
| 移動 | `WASD` | 左スティック |
| 視点 | マウス | 右スティック |
| 調べる・決定 | `E` / `Enter` | `A` |
| 懐中電灯 | `F` | `Y` |
| 走る | `Shift` | 左スティック押し込み |
| ヒント | `H` | `LB` |
| ポーズ | `Esc` / `P` | `Menu` |

タイトル画面では`Enter`、`A`または`Menu`で開始し、`Q`または`B`で終了します。  
Result画面では`R`または`X`で再挑戦、`Enter`、`A`または`Menu`でタイトルへ戻ります。

## 技術的な見どころ

- DirectX 11による独自レンダリング基盤
- 懐中電灯、天井照明、シャドウマップを組み合わせたホラー向けライティング
- ブルーム、露出、霧、ビネット、色収差、フィルム粒子などのポストプロセス
- 水たまりの平面反射、波紋、落下する水滴、濡れた床の表現
- プロシージャル形状で構成したヒューズやステージ小物
- XInputによるXboxコントローラー対応
- `std::unique_ptr`とWRL `ComPtr`を中心にした所有権・GPUリソース管理
- Update中の破棄を避ける遅延シーン遷移と安全なオブジェクト削除
- Debugビルド限定のDear ImGui調整パネル

## ビルド

1. Visual Studio 2022で`Job_application_work_horror.slnx`を開く
2. NuGetパッケージを復元する
3. 構成を`Release`、プラットフォームを`x64`にする
4. 「ソリューションのリビルド」を実行する

実行時は、生成されたexeだけでなくプロジェクトの`asset`、`shader`などの実行時データも同じ構成で配布してください。セーブデータには音量・感度・明るさ・最高記録が保存されます。

## 作品範囲

仮提出版はStage 1とStage 2を最後まで遊べる構成です。市販作品の素材や名称を複製せず、ループする空間、環境変化、観察による恐怖という心理ホラーの考え方を参考にしています。
